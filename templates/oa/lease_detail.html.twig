{% extends 'base.html.twig' %}

{% block title %} {% endblock %}

{% block header_left %}
          <div class="col-auto">
            <a class="btn btn-danger" href="{{ path('lease') }}" role="button">
            <i class="fas fa-arrow-left"></i> 返回 
            </a>
          </div>
<div class="">
<button type="button" class="btn btn-danger">
保存
</button>
</div>
{% endblock %}
{% block header_right %}
{% endblock %}

{% block main %}
<main class="container" id="">
{% for i in cols %}
  <div class="">
    <label for="{{ i.Field }}" class=""> {{ i.Comment }} </label>
    <div class="">
      <input id="{{ i.Field }}" placeholder="{{ row[i.Field] }}" type="text" class="form-control" disabled="">
    </div>
  </div>
{% endfor %}
</main>
{% endblock %}

{% block js1 %}
	<script src="/js/lease.js"></script>
{% endblock %}
